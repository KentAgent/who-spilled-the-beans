<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:WhoSpilledTheBeans"
             x:Class="WhoSpilledTheBeans.MainPage"
             BackgroundColor="Gray">
  <StackLayout x:Name="MainStackLayout" VerticalOptions="Center" HorizontalOptions="Center" Padding="40">
    <!-- Place new controls here -->
    <Label x:Name="QuoteContainer" Text="{Binding Quote}" TextColor="White"/>
    <Label x:Name="AnswerLabel1" Text="{Binding Author}" TextColor="White" />
    <Label x:Name="AnswerLabel2" Text="{Binding Author}" TextColor="White" />
    <Label x:Name="AnswerLabel3" Text="{Binding Author}" TextColor="White" />
    <Label x:Name="AnswerLabel4" Text="{Binding Author}" TextColor="White" />
    
    <Label x:Name="AuthorLabel" Text="{Binding Author}" TextColor="White" />
    <Button x:Name="StartButton" Text="Begin"
                VerticalOptions="CenterAndExpand"
                HorizontalOptions="Center"
                Clicked="OnButtonClicked" />
  </StackLayout>
</ContentPage>